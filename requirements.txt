# ติดตั้งทั้งหมด: python -m pip install -r requirements.txt
# แล้วติดตั้งเบราว์เซอร์สำหรับดึงข่าว SET: python -m playwright install chromium
yfinance==1.4.1
curl_cffi==0.15.0
python-telegram-bot[job-queue]==22.7
pandas==3.0.3
pyarrow==24.0.0
playwright==1.58.0
# อ่าน .env แบบทน BOM (utf-8-sig) — HK/US ใช้ตัวเดียวกัน
python-dotenv==1.2.2

# ใช้รันเทสต์เท่านั้น (python -m pytest) — บอทไม่ต้องใช้
pytest==9.1.1
